import React from 'react';

const AppHeader = () => (
  <header className="header header--app">
    <img className="header--app__logo" src="/images/logo.svg" alt="logo" />
  </header>
);

export default AppHeader;
